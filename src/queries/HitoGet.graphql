#import "./fragments.graphql"

query HitoGet($id: uuid!) {
  item: hito_by_pk(id: $id) {
    ...Hito
    compromiso {
      id
      titulo
    }
    conversaciones {
      ...Conversacion
    }
    actividades(order_by: { orden: asc }) {
      id
      orden
      titulo
    }
  }
}
