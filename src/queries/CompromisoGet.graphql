#import "./fragments.graphql"

query CompromisoGet($id: uuid!) {
  item: compromiso_by_pk(id: $id) {
    ...Compromiso
    hitos(order_by: { orden: asc }) {
      id
      titulo
      orden
      conversaciones {
        ...Conversacion
      }
    }
  }
}
